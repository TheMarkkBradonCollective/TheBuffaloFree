TheBuffaloFree — Play Console paste (pre-launch)

No release uploaded yet. After the first Buffalo Android build:

1. Run: npm run android:aab
2. Upload public/downloads/buf-buy-nothing-beta-v0.2.0.0001.aab (or current build output)
3. Package: org.buffalobuynothing.app
4. Add release notes when Buffalo launches — not copied from Sacramento

Closed testing URL (when configured):
  https://play.google.com/apps/testing/org.buffalobuynothing.app
